# Product

> **Fill this in first.** This file is the brief the agent works from — the stack, structure, and
> scope all follow from it. Write it with the agent: answer its questions in chat, let it update
> this file before any code exists (AGENTS.md, rule 2).

## What it is

One paragraph. What is the tool, in plain words? What does a user type or import?

## Who it's for

The people who will actually run it. Where do they get it from — a package registry, a release
binary, a container, a script?

## The problem it solves

What they do today instead, and why that's worse.

## Interface

The contract users depend on: commands/subcommands and flags, or the public API surface. Rough
is fine at this stage — this section becomes the thing you version.

## Non-goals

What this is explicitly not — the things you'll say no to.

## Success looks like

How you'll know it works: the one workflow that must feel right, and what "done" means for the
first release.
